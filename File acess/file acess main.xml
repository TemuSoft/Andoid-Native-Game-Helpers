

    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
	
	
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"/>
        </provider>
		
		
		
		
        player_one_photo.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View view) {
                Player.button(soundMute);

                fileAccess_one.showImagePickerDialog(game_two_image_0);
                editor.putString("game_two_image_0", game_two_image_0);
                editor.apply();
            }
        });
		
		
        fileAccess_one = new FileAccess(this);
        fileAccess_one.registerLaunchers(this, active_player_one_photo);
        fileAccess_one.fileName = game_one_image_0;